rootProject.name = 'GallagherMobileAccessExample'
apply from: file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); applyNativeModulesSettingsGradle(settings)
include ':app'

include ':reactnativegallaghermobileaccess'

// TODO this references the native module from the local filesystem.
// Rather, we should reference it out of node_modules like has been done in iOS
project(':reactnativegallaghermobileaccess').projectDir = new File(rootProject.projectDir, '../../react-native-gallagher-mobile-access/android')
